import {ColorType} from 'native-base/lib/typescript/components/types';

export type chartDataType = {
  name: string;
  value: number;
  color: ColorType;
  legendFontColor: ColorType;
  legendFontSize: number;
};
